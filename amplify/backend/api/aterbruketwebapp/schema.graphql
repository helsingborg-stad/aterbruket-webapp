type Advertisement @model {
  id: ID!
  title: String!
  description: String
  height: Int
  width: Int
  length: Int
  status: ItemStatus
}

enum ItemStatus {
    available
    reservec
    pickedUp
    delivered
}