type Advertisement @model {
  id: ID!
  title: String!
  description: String
  location: String
  height: Int
  width: Int
  length: Int
  status: ItemStatus
}

enum ItemStatus {
    available
    reserved
    pickedUp
    delivered
}